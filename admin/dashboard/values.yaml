ingress:
  issuer: letsencrypt-youwol
  className: kong
  host: tooling.youwol.com
  tlsSecretName: tooling-tls
  servicePort: 80

dashboard:
  metricsScraper:
    enabled: true
  serviceMonitor:
    enabled: true
  app:
    ingress:
      enabled: false
  kong:
    proxy:
      http:
        enabled: true
      tls:
        enabled: false
