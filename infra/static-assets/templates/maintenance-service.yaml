apiVersion: v1
kind: Service
metadata:
  name: {{ include "static-assets.service-maintenance.name" . }}
  labels:
    {{- include "static-assets.labels" . | nindent 4 }}
    app.kubernetes.io/component: maintenance
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "static-assets.selectorLabels" . | nindent 4 }}
