apiVersion: v2
name: assets-gateway # Will be used as docker image name
description: Deployment of youwol platform assets-gateway
type: application

# Chart version : increment when, and only when, modifying ./chart/ directory content
version: 1.3.0

# App version : increment when source code change.
# Will be used for docker image tag, unless override by value imageTage
appVersion: "0.1.12"

dependencies:
  - name: lib-backend
    version: 0.2.0
    repository: https://gitlab.com/api/v4/projects/15166395/packages/helm/stable/
