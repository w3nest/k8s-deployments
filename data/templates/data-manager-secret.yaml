{{- $name := include "data-manager.secret.name" . }}
{{- $keys := list "minio_local_secret_key" "oidc_client_secret" "google_drive_id" }}
{{- $args := dict "root" . "name" $name "keys" $keys }}
{{- include "lib-configuration.secret" $args }}
