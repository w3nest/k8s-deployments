{{- if eq .Values.jobs.manualJobs.templating "restore" }}
{{ $argsJobSpec := dict "root" . "typeJob" "manualRestore" "suspend" .Values.jobs.manualJobs.restore.suspend }}
apiVersion: batch/v1
kind: Job
metadata:
  name: restore-job
spec:
  {{- include "cluster-data-manager.backup-job-spec" $argsJobSpec | indent 2 }}
{{- end }}
